# Plaid — https://dashboard.plaid.com/developers/keys
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

# Encryption key for Plaid access tokens at rest (generate with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
TOKEN_ENCRYPTION_KEY=

# Where the SQLite file + backups live (outside the git tree)
DATA_DIR=~/budget-planner-data

PORT=8420
